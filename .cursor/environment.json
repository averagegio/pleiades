{
  "install": "test -f package.json && npm install || echo 'package.json not found — skipping install'",
  "start": "npm run dev",
  "terminals": [
    {
      "name": "Next.js dev server",
      "command": "npm run dev"
    }
  ]
}
